<h1><%=h @country.name %> <%= link_to icon('pencil'), edit_country_path(@country) %></h1>
<h2><%=h @country.native_name %></h2>


<h2>Currencies</h2>

<%= render :partial => 'currencies/browse', :locals => { :currencies => @country.currencies } %>

<h2>Authorities</h2>

<%= render :partial => 'authorities/browse', :locals => { :authorities => @country.authorities } %>

<h2>Master Banknotes <%= link_to_function icon('add'), '$("#masters_form").toggle(100)'%></h2>

<%= render :partial => 'masters/browse', :locals => { :masters => @country.masters } %>