Banknote Collection for <%= Time.now.strftime("%c")%>

Notes:

* Take grading with a pinch of salt. From best to worst: Uncirculated (UNC), About Uncirculated (AU), Extremely Fine (XF), Fine (F), Very Good (VG), Good (G), Poor (P).
* This is only a partial catalog. The rest will be coming over the next few months.

<% Country.all.each do |c| -%>
<%= "\n#{c.name.upcase}\n" %>
<% c.masters.each do |m| -%>
<% m.notes.each do |n| -%>
<%= "  %-7s %-6s %-25s %-5s %-20s" % [m.code, m.issued_on.try(:year), (n( m.denomination ) + ' ' + m.currency.unit), n.grade.try(:name), m.description] %>
<% end -%>
<% end -%>
<% end -%>