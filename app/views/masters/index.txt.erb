Master Catalog List for <%= Time.now.strftime("%c")%>

<% Region.all.each do |c| -%>
<%= "\n#{c.name.upcase}\n" %>
<% c.masters.each do |m| -%>
<%= "  %-7s %-6s %-25s %-20s" % [m.code, m.issued_on.try(:year), (n( m.denomination ) + ' ' + m.currency.unit), m.description] %>
<% end -%>
<% end -%>